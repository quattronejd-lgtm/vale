# Nozempic has moved

The Nozempic scaffold that briefly lived in this repo (branch
`claude/nozempic-content-pipeline`) has been migrated to its own repo:

**https://github.com/quattronejd-lgtm/nozempic**

Nothing Nozempic-related should be added to vale going forward. This repo
remains GNN/Harold-the-news-bot only. (Note: Nozempic's repurposer agent is
also named Harold — different agent, name collision only.)
