# Harold environment configuration
# Copy to `.env` and fill in real values. Never commit `.env`.

# ---- Instagram API (publish.js) ----
# From the Meta app's Instagram use case -> "API setup with Instagram business
# login" -> Generate access tokens: the connected account's ID and its
# long-lived token (60 days). IG_USER_ID is OPTIONAL — when unset, Harold
# resolves the account id from the token via GET /me.
# IG_USER_ID=1784xxxxxxxxxxxx
IG_ACCESS_TOKEN=IGAA...your-long-lived-token
# Instagram-business-login tokens talk to graph.instagram.com:
IG_GRAPH_BASE=https://graph.instagram.com/v21.0
# (Classic Facebook-login tokens use https://graph.facebook.com/v21.0 instead.)

# ---- Netlify public image hosting (publish.js) ----
# The Graph API can only ingest a PUBLIC image URL. Harold deploys the card to
# a DEDICATED Netlify site under a dated filename (card-YYYY-MM-DD.jpg) and
# hands Meta the public URL. Create a fresh site just for Harold (a deploy
# replaces the whole site's content — never point this at a real site).
# Token: Netlify dashboard -> User settings -> Applications -> New access token.
NETLIFY_AUTH_TOKEN=nfp_your-personal-access-token
# Site ID of the dedicated Harold site (public — it appears in the status badge).
NETLIFY_SITE_ID=1536eb41-70ea-4b6b-aa6f-54f404810ba9
# The Harold site's public URL.
PUBLIC_IMAGE_BASE=https://harold-cards.netlify.app

# ---- Editorial LLM (editorial.js) — optional ----
# If set, pickOrangeWords() uses Anthropic; otherwise a local heuristic is used.
ANTHROPIC_API_KEY=sk-ant-your-key
# HAROLD_EDITORIAL_MODEL=claude-haiku-4-5-20251001

# ---- Fetch (fetch.js) — optional ----
# Override the feed for staging/testing.
# HAROLD_FEED_URL=https://www.goodnewsnetwork.org/feed/

# ---- Rendering (render.js) — optional ----
# Path to a Chromium binary; defaults to the Playwright-managed one.
# HAROLD_CHROMIUM=/opt/pw-browsers/chromium
